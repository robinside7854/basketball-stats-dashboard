-- 094_drop_league_columns.sql
--
-- ⚠ 아직 실행하지 않은 마이그레이션입니다. 사용자 승인 후에 실행하세요.
--
-- ─────────────────────────────────────────────
-- 왜 지우는가
-- ─────────────────────────────────────────────
-- `league_columns` 는 060_league_columns.sql 로 만든 "AI 주간 칼럼(매거진)" 표입니다.
-- Claude 로 주간/월간/분기 리포트를 자동 생성 → 관리자 검토 → 발행하는 흐름을 상상하고 만들었지만,
-- 실제로는 그 흐름이 끝까지 구현된 적이 없습니다.
--
-- 코드 전수 대조 결과 (2026-08-13):
--   · `league_columns` 를 읽거나 쓰는 코드가 **애플리케이션 전체에 단 한 줄도 없습니다.**
--     (API 라우트 없음 · 서버 컴포넌트 쿼리 없음 · 클라이언트 fetch 없음)
--   · 따라서 이 표의 내용을 화면에 보여주는 페이지도 존재하지 않습니다.
--   · 현재 남아 있는 데이터는 4행뿐이며, 전부 초기 실험 흔적입니다.
--
-- 즉 이 표는 "읽는 사람이 아무도 없는 데이터"입니다. 남겨두면
--   (1) 스키마를 보는 사람이 매번 "이건 뭐지" 하고 헤매고,
--   (2) 선수 사진(cover_player_id)·본문 등 개인 관련 데이터를 목적 없이 계속 보관하게 되며,
--   (3) 백업 용량과 RLS 정책 점검 대상만 늘어납니다.
-- 그래서 정리합니다.
--
-- ─────────────────────────────────────────────
-- ⚠ 실행 전 백업 권장
-- ─────────────────────────────────────────────
-- DROP TABLE 은 되돌릴 수 없습니다. 실행 전에 반드시 아래 중 하나를 남겨 두세요.
--
--   1) Supabase 대시보드 → Database → Backups 에서 해당 시점 백업이 있는지 확인
--      (※ Supabase 백업은 "새 프로젝트로만" 복원됩니다. 같은 프로젝트에 되돌릴 수 없습니다.)
--
--   2) 4행뿐이므로 SQL Editor 에서 내용을 그대로 떠서 보관하는 편이 더 확실합니다.
--      SELECT * FROM league_columns;   -- 결과를 CSV 로 내려받아 보관
--
-- 나중에 매거진 기능을 다시 하기로 하면, 060_league_columns.sql 을 그대로 다시 실행하면
-- 같은 스키마가 복구됩니다. (데이터는 위 백업에서 되넣어야 합니다.)
--
-- ─────────────────────────────────────────────
-- 정리 대상
-- ─────────────────────────────────────────────
-- 아래 DROP TABLE 하나로 060 에서 만든 인덱스 3개
-- (idx_league_columns_league_status / _published_at / _period) 와
-- RLS 정책(league_columns_public_read_published) 이 함께 사라집니다.
-- 다른 표에서 league_columns 를 참조하는 외래키는 없으므로 CASCADE 는 쓰지 않습니다
-- (혹시 의존 객체가 남아 있다면 일부러 에러를 내서 알려주도록 두는 편이 안전합니다).

DROP TABLE IF EXISTS league_columns;
